{% extends 'posts.html.twig' %}

{% block title %}Post index{% endblock %}

{% block body %}
    {#<table class="table">#}
        {#<thead>#}
            {#<tr>#}
                {#<th>Id</th>#}
                {#<th>Title</th>#}
                {#<th>Description</th>#}
                {#<th>actions</th>#}
            {#</tr>#}
        {#</thead>#}
        {#<tbody>#}
        {#{% for post in posts %}#}
            {#<tr>#}
                {#<td>{{ post.id }}</td>#}
                {#<td>{{ post.title }}</td>#}
                {#<td>#}
                    {#{{ (post.description |raw) }}#}
                {#</td>#}
                {#<td>#}
                    {#<a href="{{ path('post_show', {'id': post.id}) }}">show</a>#}
                    {#<a href="{{ path('post_edit', {'id': post.id}) }}">edit</a>#}
                {#</td>#}
            {#</tr>#}
        {#{% else %}#}
            {#<tr>#}
                {#<td colspan="4">no records found</td>#}
            {#</tr>#}
        {#{% endfor %}#}
        {#</tbody>#}
    {#</table>#}
    <div class="row">
        <div class="col-sm-12 col-md-9 col-lg-9">
            <div class="post-index background-index">
                {% for post in posts %}
                    <h2>{{ post.title }}</h2>
                    <ul class="tags">
                        {% for tag in post.tags%}
                            <li>{{ tag }}</li>
                        {% endfor %}
                    </ul>
                    <div class="description">{{ (post.description |raw) }}</div>
                    <div class="row">
                        <div class="col-sm-6 col-md-4 col-lg-4">
                            <div class="author">
                                <span>Author:</span>
                                <a href="{{ path('user_show', {'id': post.user.id}) }}">{{ post.user.name }}</a>
                            </div>
                        </div>
                        <div class="col-sm-6 col-md-4 col-lg-3">
                            {#<div class="rating" data-post-id="{{ post.id }}">#}
                                {#{% if post.isUserEvaluated(app.user) %}#}
                                    {#{% if post.isUserAsseeement(app.user) %}#}
                                        {#<i class="fa fa-long-arrow-up active is-disabled" aria-hidden="true"></i>#}
                                        {#<div class="rating-value">#}
                                            {#{% if post.ratingPost > 0 %}#}
                                                {#<span class="text-success">+{{ post.ratingPost }}</span>#}
                                            {#{% elseif post.ratingPost < 0 %}#}
                                                {#<span class="text-danger">{{ post.ratingPost }}</span>#}
                                            {#{% else %}#}
                                                {#<span>{{ post.ratingPost }}</span>#}
                                            {#{% endif %}#}
                                        {#</div>#}
                                        {#<i class="fa fa-long-arrow-down dislike" aria-hidden="true"></i>#}
                                    {#{% else %}#}
                                        {#<i class="fa fa-long-arrow-up like" aria-hidden="true"></i>#}
                                        {#<div class="rating-value">#}
                                            {#{% if post.ratingPost > 0 %}#}
                                                {#<span class="text-success">+{{ post.ratingPost }}</span>#}
                                            {#{% elseif post.ratingPost < 0 %}#}
                                                {#<span class="text-danger">{{ post.ratingPost }}</span>#}
                                            {#{% else %}#}
                                                {#<span>{{ post.ratingPost }}</span>#}
                                            {#{% endif %}#}
                                        {#</div>#}
                                        {#<i class="fa fa-long-arrow-down active is-disabled" aria-hidden="true"></i>#}
                                    {#{% endif %}#}
                                {#{% else %}#}
                                    {#<i class="fa fa-long-arrow-up like" aria-hidden="true"></i>#}
                                    {#<div class="rating-value">#}
                                        {#{% if post.ratingPost > 0 %}#}
                                            {#<span class="text-success">+{{ post.ratingPost }}</span>#}
                                        {#{% elseif post.ratingPost < 0 %}#}
                                            {#<span class="text-danger">{{ post.ratingPost }}</span>#}
                                        {#{% else %}#}
                                            {#<span>{{ post.ratingPost }}</span>#}
                                        {#{% endif %}#}
                                    {#</div>#}
                                    {#<i class="fa fa-long-arrow-down dislike" aria-hidden="true"></i>#}
                                {#{% endif %}#}
                            {#</div>#}
                            <div class="rating-box">
                                {{ include('assessment/new.html.twig') }}
                            </div>
                        </div>
                        <div class="col-sm-6 col-md-4 col-lg-3">
                            <div class="count-comments">
                                <span>Comments: {{ post.comments|length}}</span>
                            </div>
                        </div>
                        <div class="col-sm-6 col-md-4 col-lg-2 post-btn">
                            <a href="{{ path('post_show', {'id': post.id}) }}" class="btn btn-info" role="button" aria-pressed="true">Read more</a>
                            {#<a href="{{ path('post_show', {'id': post.id}) }}" type="button" class="btn btn-light">Read more</a>#}
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>



    {{ paginator|raw }}
    <a href="{{ path('post_new') }}">Create new</a>
{% endblock %}
